events {}

http {
    server {
        listen 4000;

        location /frontend {
            proxy_pass http://client.default.svc.cluster.local;
        }

        location /backend {
            proxy_pass http://server.default.svc.cluster.local;
        }
    }
}